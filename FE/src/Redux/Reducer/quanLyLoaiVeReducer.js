const initialState = {
  ticketTypes: []
};

const quanLyLoaiVeReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default: {
      return state;
    }
  }
};

export default quanLyLoaiVeReducer;